spring.application.name=backend
# Connexion à la base de données
@spring.datasource.url=jdbc:mysql://localhost:3306/mediflow?useSSL=false&serverTimezone=UTC
spring.datasource.url=jdbc:mysql://db:3306/mediflow_db
spring.datasource.username=root

spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuration JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Hibernate détectera automatiquement la version de ton MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Port du serveur (pour éviter les conflits si tu lances plusieurs apps)
server.port=8080

# --- CONFIGURATION EMAIL (SMTP GMAIL) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sososadiqui2004@gmail.com
# ATTENTION : Ce n'est PAS ton mot de passe classique, c'est un Mot de Passe d'Application (ex: abcd efgh ijkl mnop)
spring.mail.password=bbwn mcnl ueju lvyv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true